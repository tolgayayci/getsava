/**
 * @getsava/ui — the only visual vocabulary screens compose from.
 *
 * YK-492 (T1.D0.S1) fills this in: design tokens (color / spacing / radius /
 * type scale) + the ~24-atom component inventory. Concrete token *values* are a
 * Claude Design decision delivered via the master prompt; this package owns the
 * system + names. Placeholder export below keeps the package importable so
 * workspace wiring can be verified before the tokens land.
 */
export const UI_PACKAGE = '@getsava/ui';
