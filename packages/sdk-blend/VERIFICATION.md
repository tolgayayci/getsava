# BLEND_INTEGRATION.md — Adversarial Verification

**Verifier method:** Each claim re-checked independently against (a) the installed SDK source `@blend-capital/blend-sdk@3.2.2` (`reference-mvp/stellarkasa/node_modules/.../dist/cjs/*.d.ts` + `.js`), (b) the official Blend v2 contract source `github.com/blend-capital/blend-contracts-v2` (`pool/src/pool/actions.rs`, `pool/src/pool/reserve.rs`, `pool/src/contract.rs`), (c) `soroban-fixed-point-math` crate, (d) live Stellar **testnet** Soroban RPC simulate calls, (e) npm registry, (f) `docs.blend.capital`.

**Tally: 20 CONFIRMED · 3 UNCERTAIN · 1 WRONG (citation-level, conclusion still correct)**

## Verdict table

| # | Claim (spec section) | Verdict | Evidence / corrected value |
|---|---|---|---|
| 1 | RequestType **names** Supply/Withdraw/SupplyCollateral/WithdrawCollateral/Borrow/Repay/fills | ✅ CONFIRMED | SDK `pool/index.d.ts` enum + contract `actions.rs:23-33` identical. |
| 2 | RequestType **numeric** SupplyCollateral=2, WithdrawCollateral=3, Borrow=4, Repay=5, fills 6-9 | ✅ CONFIRMED | SDK `pool/index.js:54-63` + contract `actions.rs` `#[repr(u32)]` + `from_u32` match exactly. |
| 3 | `assertSafeRequestType` allows only {2,3}; borrow/repay/auctions unreachable | ✅ CONFIRMED | Values correct; logic sound. |
| 4 | `submit({from,spender,to,requests})` → base64 op string; `PoolContractV2` (not abstract `PoolContract`) | ✅ CONFIRMED | SDK `pool_contract.d.ts`: `SubmitArgs{from,spender,to,requests}`, `submit(args):string`; `PoolContract` is `abstract`, `PoolContractV2` concrete. README example uses identical shape + plain `submit`. |
| 5 | `Request = {request_type, address, amount: i128(bigint)}`; USDC amount = `human*1e7` (7 dec) | ✅ CONFIRMED | SDK `Request` interface exact; live `get_reserve(USDC).config.decimals = 7`. |
| 6 | Full-withdraw via `I128MAX` = `170141183460469231731687303715884105727` (SDK export) | ✅ CONFIRMED | `index.js:30 I128MAX=BigInt('1701411...727')` = 2^127−1 (node-verified). |
| 7 | **I128MAX clamps to position on-chain, no revert / no 0-withdraw** | ✅ CONFIRMED | Contract `actions.rs:371-378` WithdrawCollateral: `to_burn=to_b_token_up(amount); if to_burn>cur_b_tokens {to_burn=cur_b_tokens; tokens_out=to_asset_from_b_token(cur_b_tokens)}`. Clamps to `user.get_collateral(index)`. |
| 8 | I128MAX won't **overflow** in `to_b_token_up` | ✅ CONFIRMED (not flagged by spec — good news) | `reserve.rs` uses `SorobanFixedPoint` (Env trait) → promotes to **I256** for the `mul` (`i256.rs:39 mul_div_ceil`); `I128MAX×1e12≈2^167` fits I256; result `≈I128MAX/1.0008 < I128MAX` fits i128 on narrow-back. No panic. |
| 9 | Source-account auth suffices; **NO** authorizeEntry / per-entry signing (from===source===signer) | ⚠️ UNCERTAIN→OK (citation WRONG, conclusion CORRECT) | Contract `contract.rs:452-463` calls **`spender.require_auth(); if from!=spender {from.require_auth()}`** — NOT `from.require_auth()` as spec §1/§4-step7 state. For Sava (from=spender=to=source=signer) only `spender.require_auth` fires and is satisfied by the `SourceAccount` envelope sig. **Outcome correct; fix the wording to "spender.require_auth (and from when from≠spender)".** |
| 10 | Plain `submit` (inline transfer auth, no SAC approve) — not `submitWithAllowance` | ✅ CONFIRMED | README example uses `submit`; `execute_submit(...,false)` path. `submitWithAllowance` exists but is the allowance variant. Resolves §11-UNVERIFIED-6. |
| 11 | Testnet RPC `https://soroban-testnet.stellar.org` | ✅ CONFIRMED | Live simulate calls succeeded against it. |
| 12 | Passphrase `Test SDF Network ; September 2015` | ✅ CONFIRMED | `Networks.TESTNET` === spec string (node-verified). |
| 13 | Chosen pool `CAPBMXIQ…` live, `is_pool`/active | ✅ CONFIRMED | Live `get_config().status = 0` (active); Stellar Expert shows deployed wasm. **Testnet only** (not mainnet). |
| 14 | USDC SAC `CBIELTK6…` = deterministic SAC of Circle `USDC:GBBD47IF…` | ✅ CONFIRMED | `Asset('USDC','GBBD47…').contractId(TESTNET)` === `CBIELTK6…` (node-verified); Stellar Expert maps it to `USDC-GBBD47IF…-1`. **Testnet.** |
| 15 | USDC is reserve **index 1** in chosen pool, `enabled`, 7-dec | ✅ CONFIRMED | Live `get_reserve_list` → `[XLM, CBIELTK6(USDC), …, …]`, index 1; `get_reserve(USDC).config.enabled=true, decimals=7, index=1`. |
| 16 | Canonical `TestnetV2 CCEBVDYM…` + Blend USDC `CAQCFVLO…`/oracle `CAZOKR2Y…` (the *alternative*) | ✅ CONFIRMED | Live `get_config(CCEBVDYM).status=0`, oracle=`CAZOKR2Y…` matches spec. Real testnet. |
| 17 | bRate is **V2 scaled 1e12** (rateDecimals=12); V1=1e9 | ✅ CONFIRMED | SDK `reserve.js:580 rateDecimals=12` (V2), `:413 =9` (V1). Live `get_reserve(USDC).data.b_rate = 1000768010169` (≈1.0008 ×1e12). |
| 18 | Supply APY = `estSupplyApy`, weekly-compounded `(1+supplyApr/52)^52−1` | ✅ CONFIRMED | SDK `reserve.js:341 estSupplyApy=(1+supplyApr/52)**52-1`. Borrow APY `/365` daily (`:340`). |
| 19 | `supplyApr = borrowApr × U × (1−backstopTakeRate)`; backstopRate from `pool.metadata.backstopRate` | ✅ CONFIRMED | SDK `reserve.js:336-338`: `supplyCapture=(SCALAR_7−take)×U; supplyApr=curIr×supplyCapture` ≡ spec (commutative). Live `bstop_rate=5000000`=0.5. Piecewise IR + "r_three NOT ×RM" matches `:315-333`. |
| 20 | Empty reserve (U=0) pays 0% supply | ✅ CONFIRMED | `reserve.js:304-307` `if curUtil==0 {supplyApr=0}`. |
| 21 | `PoolV2.load(network,id)` static (not on abstract `Pool`); auto-accrues rates; `pool.metadata.status:number`, `reserves: Map`, `loadOracle/loadUser` | ✅ CONFIRMED | SDK `pool.d.ts`: `PoolV2.load` static; `Pool` abstract; `pool_metadata.d.ts` has `status:number, backstopRate:number, reserveList:string[], oracle:string`. Network type `{rpc,passphrase,opts?}` matches. |
| 22 | SupplyCollateral lands in **collateral** map → read via `getCollateral*` not `getSupply*` | ✅ CONFIRMED | SDK `pool_user.d.ts` has both `getCollateralFloat/BTokens` and `getSupply*`; contract `actions.rs` WithdrawCollateral reads `user.get_collateral` — consistent. Strong catch by spec. |
| 23 | stellar-sdk version: blend-sdk@3.2.2 declares `@stellar/stellar-sdk@14.4.3` (exact); use `/rpc` subpath | ⚠️ UNCERTAIN | blend-sdk `package.json` pins **exact `14.4.3`** (→ stellar-base `^14.0.4`), npm-confirmed, `/rpc` export exists. BUT reference-mvp actually has **`@stellar/stellar-sdk@15.0.1` / stellar-base@15.0.0** installed → the dual-major conflict the spec warns about is REAL. A `pnpm.overrides` to 14 will override blend-sdk's exact pin (fine); to 15 would override blend-sdk's own dep (riskier). Validate one resolved stellar-base post-install. |
| 24 | blend-sdk@3.2.2 latest, published 2025-12-30 | ✅ CONFIRMED | npm registry: latest=3.2.2, time=2025-12-30T15:13:48Z. |
| 25 | Pool status integer enum 0..6 (Admin_Active=0 … Setup=6) + supply/withdraw matrix | ⚠️ UNCERTAIN | NOT a typed enum in SDK or contract source seen; only `setStatus` docstring confirms admin sets **0=active / 2=on-ice / 4=frozen**. The 0/2/4 anchors are CONFIRMED by `contract.rs`/`pool_contract.d.ts`; the odd states 1/3/5 and Setup=6 + the per-state withdraw matrix are inferred. Spec already flags this (§11). Decode `get_config().status` live before trusting the matrix; don't hard-block withdraw on a guessed state. |

## MUST-FIX before implementation (prioritized)

1. **[P0 — single stellar-base] Resolve the 14 vs 15 split for real.** blend-sdk@3.2.2 hard-pins `@stellar/stellar-sdk@14.4.3`, but the existing tree resolves **15.0.1 / stellar-base@15.0.0**. Two stellar-base majors → `instanceof`/XDR-identity failures on `tx.hash()` / `addSignature` / `assembleTransaction` across the package boundary. Add a root `pnpm.overrides` forcing **exactly one** `@stellar/stellar-base` (align to 14.x to match what blend-sdk is built against, OR move everything to 15 and accept overriding blend-sdk's pin), then assert a single resolved version after install. Always cross package boundaries with **base64 XDR strings**, never live objects. (Claim 23)

2. **[P1 — auth wording, get the reason right] Fix the §1/§4-step-7 auth rationale.** The contract authorizes via **`spender.require_auth()` (plus `from.require_auth()` only when `from != spender`)**, not "`from.require_auth()`". The spec's *conclusion* (source-account credential suffices, no `authorizeEntry`) is correct ONLY because Sava sets `from = spender = to = source = signer`. Keep that invariant hard-enforced in `buildSupplyTx`/`buildWithdrawTx`; if a fee-payer/source ever differs from `spender`, the envelope sig no longer authorizes the transfer and you'd need `SorobanAddressCredentials` + `authorizeEntry`. Correct the docstring so a future maintainer doesn't break it. (Claim 9)

3. **[P1 — don't trust the guessed status matrix; verify at startup] Pool status 1/3/5/6 + withdraw-availability matrix is inferred, not from source.** Only 0/2/4 are confirmed. At startup do the live asserts the spec already prescribes — `is_pool(poolId)` (factory authenticity), `get_reserve_list` (USDC present), `get_reserve(USDC).config.enabled` — and read `get_config().status` live; do NOT hard-block withdraw based on a status value you mapped by guess. Also note testnet resets wipe state, so `CAPBMXIQ…` (community/custom pool, not in blend-utils) may rotate — keep the canonical `TestnetV2 CCEBVDYM…` path as fallback. (Claims 13, 25)

## Notes for confidence
- I128MAX full-withdraw is the strongest result: clamping confirmed in the *actual contract* (`actions.rs:371-378`) and overflow-safety confirmed via the I256-promoting fixed-point crate. It will withdraw the full position and will NOT revert or withdraw 0. Still worth one live testnet round-trip (deposit → withdraw I128MAX) per §11-UNVERIFIED-5.
- APY math matches the SDK line-for-line; reading `reserve.estSupplyApy` after `PoolV2.load` is correct and will track chain within tolerance. Emissions leg is correctly treated as optional/lower-bound — confirm `reserve.supplyEmissions !== undefined` + a BLND testnet price at runtime before showing an emissions APY.
- Addresses, passphrase, RPC, USDC SAC↔issuer reconciliation, decimals, reserve index 1, bRate 1e12 scale — all live-verified on testnet and currently deployed.
