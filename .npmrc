# Expo + Metro resolve modules from a flat node_modules tree, so use the hoisted
# linker rather than pnpm's default isolated/symlinked store.
node-linker=hoisted
# React Native's dependency graph has many loose peer ranges; don't hard-fail.
auto-install-peers=true
strict-peer-dependencies=false
