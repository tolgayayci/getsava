# Expo public env — inlined into the client bundle. NEVER put secrets here.
# Copy to apps/mobile/.env.local for local development.
#
# EXPO_PUBLIC_API_URL=http://localhost:8787
# EXPO_PUBLIC_PRIVY_APP_ID=
