# Dependencies
node_modules/
.pnpm-store/

# Build output / caches
dist/
build/
.next/
.nuxt/
.turbo/
.parcel-cache/
.cache/
coverage/
*.tsbuildinfo

# Expo / React Native
.expo/
apps/*/.expo/
*.orig.*
web-build/
# Generated native projects (Expo prebuild / CNG)
apps/*/ios/
apps/*/android/

# Cloudflare Workers / Wrangler
.wrangler/
.dev.vars
.dev.vars.*

# Env (secrets never committed; .env.example stays tracked)
.env
.env.local
.env.*.local

# Claude Code runtime artifacts
.claude/scheduled_tasks.lock

# Editors / OS
.DS_Store
*.log
.idea/
.vscode/*
!.vscode/extensions.json

# Python (legacy/common)
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
target/
vendor/
tmp/

# Claude Code runtime artifacts (scheduled-task lock, etc.)
.claude/scheduled_tasks.lock

# Read-only reference MVP (kept on disk, never committed) — see README
reference-mvp/

# Local-only project guidance (kept on disk, never committed)
/CLAUDE.md

# Local-only web design-system mirror for claude.ai/design (react-native-web bridge)
/design-web/
# Local-only claude.ai/design sync config (points at /design-web)
/.design-sync/

# Local-only D5 cohort simulation (testnet E2E proof + report) — never committed
/cohort-sim/
packages/sdk-blend/scripts/cohort-sim.ts
packages/sdk-blend/scripts/position.ts
packages/sdk-blend/scripts/reward-zone.ts

# Scraped third-party docs (research notes, kept local only)
.firecrawl/

# Generated Postman environment (contains a local session token)
apps/backend/postman/sava-local.postman_environment.json
